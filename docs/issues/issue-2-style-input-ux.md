# feat: update style input placeholder and label in chat UI

**Type:** AFK  
**Blocked by:** None — parallelisable with issue-1

## What to build

Update the message input in ChatWindow to clearly invite style descriptions. The mock response can remain in place — this slice is purely a UI copy/UX change and is demoable without a live backend.

## Acceptance criteria

- [ ] Message input placeholder reads something like "Describe your style — casual, smart, love layers…"
- [ ] Input is still optional (empty submit works)
- [ ] No functional regression — mock flow still works
